@model List<MyFirstBlog.Models.Entities.Article>

@{
    ViewBag.Title = "Home Page";
}

@*<div class="jumbotron">
    <h1>ASP.NET</h1>
    <p class="lead">ASP.NET is a free web framework for building great Web sites and Web applications using HTML, CSS and JavaScript.</p>
    <p><a href="https://asp.net" class="btn btn-primary btn-lg">Learn more &raquo;</a></p>
</div>*@

<div class="row">

    @foreach (var item in Model)
    {
       <div class="panel">
           <div class="panel-body">
               <article>
                   <h3>@item.Title</h3>
                   <label class="label label-info">@item.Category.Name - @item.CreatedDate.ToShortDateString()</label>
                   <p>

                       @MyFirstBlog.Infra.ToolKit.SafeSubstring(item.Content, 0, 200)

                       <a href="@Url.Action("Detail","Article",new { id=item.Id})"> devamı...</a>

                   </p>
               </article>
           </div>
       </div>

    }

</div>